import Start from './Start'

export default function Navbar() {
  return (
    <nav className="bg-windows-gray-primary fixed bottom-0 left-0 flex h-8 w-full items-center justify-between px-1">
      <Start />
    </nav>
  )
}
